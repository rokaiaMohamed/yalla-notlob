<%= form_with(model: group_user, local: true) do |form| %>
  <% if group_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_user.errors.count, "error") %> prohibited this group_user from being saved:</h2>

      <ul>
      <% group_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :group_user_user_id %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.text_field :group_id, id: :group_user_group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
